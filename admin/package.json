{
  "name": "marvel-admin",
  "version": "11.8.0",
  "private": true,
  "author": "RedQ",
  "scripts": {
    "clean": "yarn run rimraf -g \"{.next,node_modules,__generated__,.cache,src/graphql/*.d.ts,src/framework/graphql/**/*.d.ts}\" && rimraf node_modules",
    "dev": "yarn dev",
    "build": "yarn build",
    "start": "yarn start"
  },
  "devDependencies": {
    "prettier": "3.0.3",
    "rimraf": "3.0.2"
  }
}
